# Environment Configuration for The Meta-Broker
# Copy this file to .env and customize for your deployment

# Production/Development mode
NODE_ENV=development
# For production deployment, set to: NODE_ENV=production

# Domain configuration
DOMAIN=themetabroker.org

# Port configuration (for production, this is usually set by the hosting provider)
PORT=3000
